console.clear();
// Here, we've created mod.js as a module.
const pi = 3.14;
// To use variables from a module, we have to export them using the export keyword
// and specify default to export only one object.
export default pi;
