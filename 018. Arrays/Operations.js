console.clear();

const toDo = ["call mom", "dishes"];
toDo.push("groceries", "feed cat");
console.log(toDo);

toDo.pop();
console.log(toDo);

const tasks = ["dishes", "windows", "vacuum"];
const length = tasks.length;
console.log(length);

const numbers = [16, 43, 66, 21];
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}
